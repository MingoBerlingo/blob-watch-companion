# Display Fonts

This folder contains font tables used by the native paint/display pipeline.

Fonts are kept separate from the core display driver logic so they can be replaced,
trimmed, or expanded without touching rendering/transport code.

Related docs:

- [../README.md](../README.md): Display layer overview
